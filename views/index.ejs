<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/index.css">
</head>
<body>
    <h1 id="title">Quoera Posts</h1>
    <% for(let post of posts) { %>
        <div class="post_box">
            <h3>Title : <%= post.title %></h3>
            <p>Content : <%= post.content %></p>
            <h5>Author : <%= post.author %></h5>
            <h6>UpVotes : <%= post.upvotes %></h6>
            <h6>Comments : <%= post.comments %></h6>
            <button>Upvote</button>
            <div class="tags">
                <% for(let str of post.tags) { %>
                    <p class="tag_design"><%= str %></p>
                <% } %>
            </div>
            <div class="tags">
                <button class="view">View</button>
                <button class="edit">Edit</button>
                <form method="POST" action="http://localhost:8080/posts/<%= post.id %>?_method=DELETE" class="delete">
                    <button type="submit">Delete</button>
                </form>            
            </div>
        </div>
    <% } %>
    <button class="create"> Add Post </button>
</body>
<script src="js/index.js"></script>
</html>